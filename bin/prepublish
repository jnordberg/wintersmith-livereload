#!/bin/bash

# compile

echo "Compiling CoffeeScript..."

if [ -f ./node_modules/.bin/coffee ]; then
    ./node_modules/.bin/coffee -o ./lib -b -c ./src
    exit $?
else
  echo "CoffeScript package missing. Run npm install"
  exit 1
fi
